name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 0 * * 0"  # Every Sunday at midnight UTC
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Vercel app
        run: curl -f https://tv-time-2.vercel.app || exit 1
